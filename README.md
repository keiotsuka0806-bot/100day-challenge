# 100day-challenge

Small products, prototypes, notes, and experiments created through a 100-day building challenge.

This repository is a working archive. It contains not only finished apps, but also planning notes, prototypes, design reviews, QA notes, and operation logs.

## Main Areas

- `開発部/`: apps and prototypes
- `企画部/`: ideas, specs, and research notes
- `デザイン部/`: design reviews and visual notes
- `QA部/`: testing and review material
- `運用部/`: daily operations, tasks, reports, and process notes
- `広報部/`: public-facing drafts and handoff notes
- `記憶庫/`: project memory and reference notes

## App Prototypes

The main app/prototype directories are under `開発部/`.

- `food-score`
- `quote-court`
- `lexworld`
- `wine-board-game`
- `mood-forecast`
- `whisky-note`
- `nani-taberu`
- `tatsuro`
- `mini-kanban`
- `sound-frame`

Some are simple static prototypes, while others use tools such as Vite, React, Firebase, or Node scripts.

## Mood Forecast

The `開発部/mood-forecast` directory in this repository is an early web prototype.

The ongoing, canonical Mood Forecast project now lives in its own repository:

```text
https://github.com/keiotsuka0806-bot/mood-forecast
```

Use that repository for current Mood Forecast development.

## How To View

Many prototypes can be inspected by opening their `index.html` files or by running the project-specific commands in each app directory.

For Node/Vite-based projects, the usual flow is:

```bash
npm install
npm run dev
```

Check each app directory for its own `package.json` or README when available.

## Status

This is an active learning and building workspace. Expect rough edges, experiments, and incomplete ideas alongside more polished work.
