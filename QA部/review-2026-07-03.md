# QAレビュー 2026-07-03 — なんで箱（naze-box）

対象: `開発部/naze-box`（デプロイ前ゲート2 / QA）

## 結果: **GO**（正確性バグ 0件）

### 実施アングル（インライン）
- 行単位スキャン / 削除挙動の監査 / クロスファイル参照 / 再利用・簡素化・効率・アルティチュード
- app.js が参照する全DOM ID（24個）が index.html に存在することを実測確認
- 削除した関数・ID（buildTreeSVG / stageFor / TREE_SIZE / STAGES / DECO_SLOTS / tree-scene / grow-fill 等）への残存参照が無いことを grep で確認

### 対応済み
- 🟡 回答待ち中の二重送信ガード（`asking` フラグ＋ask-btn無効化）を追加＝重複OpenAI呼び／競合を防止（ゲート1で実装）

### 確認した正常系
- XSS: ユーザー入力は `escapeHtml`（箱カード）と `textContent`（回答）で描画。`innerHTML` に生入力を渡していない
- APIキー: クライアント露出なし（`/api/ask` サーバ関数内のみ）。AIカテゴリはCATEGORIES allowlistで検証
- 状態網羅: 読込(スピナー)／空(箱・森)／失敗(モックfallback、alert不使用)
- 森ロジック: 分野別カウント→段階→SVG／grow-bar／訪問判定（新分野=確定・活動=たまに）に添字/境界バグなし

→ セキュリティゲート（ゲート3）へ。

---

## セキュリティチェック（ゲート3）: **92/100 GO**
- 鍵露出なし（サーバ関数内のみ・.env無し）／XSSなし（escapeHtml＋textContent）／eval無し／インジェクション無し（localStorageのみ）／カテゴリはallowlist検証／文字数上限＋簡易レート制限＋鍵なしモック
- 🟡MEDIUM 1件（コードの穴ではない）: サーバーレスのレート制限はインスタンス単位の簡易ガード → **OpenAI側の月額Usage limits設定を推奨**（公開後可）
- コード修正: 不要 → **3ゲート通過**
