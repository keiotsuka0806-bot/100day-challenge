# QAレビュー — 自由研究ラボ（jiyukenkyu-lab）2026-07-11

4視点並列レビュー（正確性 / データ消失 / API・サーバー / UI規約）。検出29件（高3・中12・低14、うち確認済み問題なし4件含む）→**修正20件・許容5件（理由付き）**。修正後に高リスク3件をブラウザで回帰実測。判定: **GO**

## 正確性（修正7件）
1. ✅修正【高】「つづきから」が常に観察ラボへ飛ぶ（init末尾のshow('home')がstepを上書き→リスナーが常に'home'を読む）→ resumeStepを退避して使用。**回帰実測: ②bで中断→リロード→つづきから→②bに復帰・問いも表示**
2. ✅修正【高】「はじめる」リセット後に旧研究のDOM残留テキストが新研究へ混入 → clearInputs()で全入力欄・ヒント・outcome選択をクリア。**回帰実測: リセット後に全欄空**
3. ✅修正【中】リロード後②の「🔬テーマ:」表示が空（pickedTheme復元漏れ）→ show('question')で復元
4. ✅修正【中】show('themes')がカード再描画しない（バグ1修正で顕在化する潜在バグ）→ renderThemes()を呼ぶ
5. ✅修正【中】入力途中テキストが確定前のリロードで消える → 4つのtextareaにchange下書き保存
6. ✅修正【低】テーマ再選択時に入力中の問いをtoi_hintで無言上書き → DOM値もチェック
7. ✅修正【低】サーバーエラー/429を「オフライン」と誤表示（絶対ルール1）→ reason分岐（busy/ai_error/offline）＋はかせ文言

## データ消失（修正5件・許容1件）
1. ✅修正【高】写真60件はlocalStorage 5MB制限に収まらない（典型6MB前後・実際は40件前後で停止）→ JPEG品質0.75→0.62＋容量3.8MB超で先回り警告トースト（巻き戻し機構は既存で正しく動作）
2. ✅修正【中】記録追加以外のsave()失敗時に巻き戻しなしで先へ進む → 全保存関数で `if (!save()) return;`
3. ✅修正【中】正当JSONだが形が壊れたデータで起動不能（"null"等でTypeError→操作不能・退避も走らない）→ load()でfreshStateマージ＋型検査。**回帰実測: "null"を注入→_brokenへ退避→正常起動**
4. ✅修正【低】絵文字がslice境界で「�」化（サロゲートペア分断）→ cut()（コードポイント単位）に統一
5. ✅修正【低】SWがアイコンをprecacheしない → ASSETSに追加・キャッシュ名v2
6. ⚠️許容 recMemo（観察メモ）の下書きは保存しない（1行メモで打ち直しが容易・記録は追加時に確定保存されるため）

## API・サーバー（修正4件・許容3件）
1. ✅修正【中】タイムアウト序列の逆転（クライアント45s<サーバー50s＝切断後もOpenAI課金だけ走る）→ サーバー中断40sに
2. ✅修正【中】429をクライアントが「オフライン」表示 → busy分岐＋「混み合っとる」文言
3. ✅修正【低】ipHits 5000超で全消し（per-IP制限の一斉リセット）→ 期限切れのみ間引き
4. ✅修正【低】OpenAI応答の要素レベル形状検証なし（空タイトルカード/"[object Object]"表示）→ filter＋空ならthrowでモックへ
5. ⚠️許容 レート制限はインスタンス内メモリ（既公開アプリと同型・OpenAI月額上限が最終の砦。コメントに明記）
6. ⚠️許容 ヘッダIPはVercel配下前提（コメントに明記）
7. ⚠️許容 max_tokens 1200（切断時はai_errorモックに安全に縮退する設計のため現状可）

## UI規約（修正4件・許容1件）
1. ✅修正【中】タップターゲット44px未満×3（.chip 40px / .step-nav 36px / .rec-del 32px）→ すべて44pxに
2. ✅修正【中】印刷に「はかせ吹き出し・よそう答え合わせ・見出し」が混入 → @media printに追加
3. ✅修正【低】SWのnavigationフォールバックがundefinedを返しうる → `|| Response.error()`
4. ✅修正【低】/api/判定がサブパス配信で効かない → includes判定に
5. ⚠️許容 og:imageが相対パス → デプロイ後に本番絶対URLへ更新する（デプロイ手順に組込み）

## 総括
- 高3件はすべて修正＋ブラウザ回帰実測済み。データ破壊系は「巻き戻し＋退避＋先回り警告」の3層に
- 「事実と違う表示をしない」（絶対ルール1）系の指摘が2件あり、いずれもreason分岐で解消
